rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Wildcard match for user-isolated collections
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
